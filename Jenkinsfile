pipeline {
    agent any
    options { 
            timestamps() 
            overrideIndexTriggers(true)     
    }
    
    stages {
        stage("Build") {
            steps {
                echo "Building on Hello World"
            }
        }
    }
}
